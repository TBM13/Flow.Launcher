<Window
    x:Class="Flow.Launcher.Windows.CustomQueryHotkeySetting"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Flow.Launcher.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    Title="Custom Query Hotkey"
    Width="530"
    ui:WindowHelper.UseModernWindowStyle="True"
    Background="{DynamicResource PopuBGColor}"
    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    Foreground="{DynamicResource PopupTextColor}"
    Icon="/Images/app.png"
    MouseDown="window_MouseDown"
    ResizeMode="NoResize"
    SizeToContent="Height"
    WindowStartupLocation="CenterScreen"
    WindowStyle="ToolWindow">
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="Close" />
    </Window.InputBindings>
    <Window.CommandBindings>
        <CommandBinding Command="Close" Executed="cmdEsc_OnPress" />
    </Window.CommandBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="80" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="26 10 26 0">
            <TextBlock
                Margin="0 0 0 12"
                FontSize="20"
                FontWeight="SemiBold"
                Text="Custom Query Hotkey"
                TextAlignment="Left" />
            <TextBlock
                FontSize="14"
                Text="Press a custom hotkey to open Flow Launcher and input the specified query automatically."
                TextAlignment="Left"
                TextWrapping="WrapWithOverflow" />
            <Image
                Width="478"
                Margin="0 20 0 0"
                Source="/Images/illustration_01.png" />

            <Grid Width="478" Margin="0 20 0 0">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    Grid.Row="0"
                    Grid.Column="0"
                    Margin="10"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    FontSize="14"
                    Text="Hotkey" />
                <controls:HotkeyControl
                    x:Name="HotkeyControl"
                    Grid.Row="0"
                    Grid.Column="1"
                    Grid.ColumnSpan="2"
                    Margin="10 0 10 0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    HorizontalContentAlignment="Left" />
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="0"
                    Margin="10"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    FontSize="14"
                    Text="Query" />
                <TextBox
                    x:Name="tbAction"
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="10"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    Text="{Binding ActionKeyword}" />
                <Button
                    x:Name="btnTestActionKeyword"
                    Grid.Row="1"
                    Grid.Column="2"
                    Margin="0 0 10 0"
                    Padding="10 5 10 5"
                    Click="BtnTestActionKeyword_OnClick"
                    Content="Preview" />
            </Grid>
        </StackPanel>
        <Border
            Grid.Row="1"
            Margin="0 14 0 0"
            Background="{DynamicResource PopupButtonAreaBGColor}"
            BorderBrush="{DynamicResource PopupButtonAreaBorderColor}"
            BorderThickness="0 1 0 0">
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <Button
                    x:Name="btnCancel"
                    MinWidth="140"
                    Margin="10 0 5 0"
                    Click="BtnCancel_OnClick"
                    Content="Cancel" />
                <Button
                    x:Name="btnAdd"
                    MinWidth="140"
                    Margin="5 0 10 0"
                    Click="btnAdd_OnClick"
                    Style="{StaticResource AccentButtonStyle}">
                    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <TextBlock
                            x:Name="tbAdd"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="Done"
                            Visibility="Collapsed" />
                        <TextBlock
                            x:Name="tbUpdate"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="Update"
                            Visibility="Collapsed" />
                    </Grid>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
