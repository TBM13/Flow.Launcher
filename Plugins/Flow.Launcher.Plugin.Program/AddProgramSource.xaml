<Window
    x:Class="Flow.Launcher.Plugin.Program.AddProgramSource"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:root="clr-namespace:Flow.Launcher.Plugin.Program"
    xmlns:vm="clr-namespace:Flow.Launcher.Plugin.Program.ViewModels"
    Title="{x:Static root:Localize.Settings_Directory}"
    Width="Auto"
    Height="276"
    d:DataContext="{d:DesignInstance vm:AddProgramSourceViewModel}"
    Background="{DynamicResource PopuBGColor}"
    Foreground="{DynamicResource PopupTextColor}"
    ResizeMode="NoResize"
    SizeToContent="Width"
    WindowStartupLocation="CenterScreen"
    WindowStyle="ToolWindow"
    mc:Ignorable="d">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="80" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="26 0 26 0">
            <StackPanel Grid.Row="0" Margin="0 0 0 12">
                <TextBlock
                    Grid.Column="0"
                    Margin="0 0 0 0"
                    FontSize="20"
                    FontWeight="SemiBold"
                    Text="{x:Static root:Localize.ProgramSource_EditTitle}"
                    TextAlignment="Left" />
            </StackPanel>
            <StackPanel>
                <TextBlock
                    FontSize="14"
                    Text="{x:Static root:Localize.ProgramSource_EditTips}"
                    TextAlignment="Left"
                    TextWrapping="WrapWithOverflow" />
            </StackPanel>

            <StackPanel Margin="0 10 10 0" Orientation="Horizontal">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="0"
                        Margin="10"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        FontSize="14"
                        Text="{x:Static root:Localize.Settings_Directory}" />
                    <DockPanel
                        Grid.Row="0"
                        Grid.Column="1"
                        LastChildFill="True">
                        <Button
                            MinWidth="70"
                            HorizontalAlignment="Stretch"
                            Click="BrowseButton_Click"
                            Content="{x:Static root:Localize.Settings_Browse}"
                            DockPanel.Dock="Right"
                            Visibility="{Binding IsCustomSource, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <TextBox
                            Name="Directory"
                            Width="350"
                            Margin="10"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Center"
                            IsReadOnly="{Binding IsNotCustomSource}"
                            Text="{Binding Location, Mode=TwoWay}" />
                    </DockPanel>
                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="0"
                        Margin="10"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        FontSize="14"
                        Text="{x:Static root:Localize.Settings_ProgramEnabled}" />
                    <CheckBox
                        x:Name="Chkbox"
                        Grid.Row="1"
                        Grid.Column="1"
                        Margin="10 0"
                        VerticalAlignment="Center"
                        IsChecked="{Binding Enabled, Mode=TwoWay}" />
                </Grid>
            </StackPanel>
        </StackPanel>
        <Border
            Grid.Row="1"
            Margin="0 14 0 0"
            Background="{DynamicResource PopupButtonAreaBGColor}"
            BorderBrush="{DynamicResource PopupButtonAreaBorderColor}"
            BorderThickness="0 1 0 0">
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <Button
                    x:Name="btnCancel"
                    MinWidth="140"
                    Margin="10 0 5 0"
                    Click="BtnCancel_OnClick"
                    Content="Cancel" />
                <Button
                    x:Name="btnAdd"
                    MinWidth="140"
                    Margin="5 0 10 0"
                    Click="BtnAdd_OnClick"
                    Content="{Binding AddBtnText}"
                    Style="{DynamicResource AccentButtonStyle}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>