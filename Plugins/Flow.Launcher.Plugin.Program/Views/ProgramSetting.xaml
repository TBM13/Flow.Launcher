<UserControl
    x:Class="Flow.Launcher.Plugin.Program.Views.ProgramSetting"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:root="clr-namespace:Flow.Launcher.Plugin.Program"
    Height="520"
    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <Style x:Key="SettingPanelSeparatorStyle" TargetType="Separator">
            <Setter Property="Margin" Value="-70 13.5 -18 13.5" />
            <Setter Property="Height" Value="1" />
            <Setter Property="VerticalAlignment" Value="Top" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DockPanel HorizontalAlignment="Stretch" LastChildFill="True">
            <TextBlock
                MinWidth="120"
                VerticalAlignment="Center"
                Text="{x:Static root:Localize.Settings_IndexSources}" />
            <WrapPanel
                Width="Auto"
                HorizontalAlignment="Right"
                DockPanel.Dock="Right">
                <CheckBox
                    Name="UWPEnabled"
                    Content="{x:Static root:Localize.Settings_IndexUwp}"
                    IsChecked="{Binding EnableUWP}"
                    ToolTip="{x:Static root:Localize.Settings_IndexUwp_Tooltip}"
                    Visibility="{Binding ShowUWPCheckbox, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <CheckBox
                    Name="StartMenuEnabled"
                    Content="{x:Static root:Localize.Settings_IndexStart}"
                    IsChecked="{Binding EnableStartMenuSource}"
                    ToolTip="{x:Static root:Localize.Settings_IndexStart_Tooltip}" />
                <CheckBox
                    Name="RegistryEnabled"
                    Content="{x:Static root:Localize.Settings_IndexRegistry}"
                    IsChecked="{Binding EnableRegistrySource}"
                    ToolTip="{x:Static root:Localize.Settings_IndexRegistry_Tooltip}" />
                <CheckBox
                    Name="PATHEnabled"
                    Content="{x:Static root:Localize.Settings_IndexPATH}"
                    IsChecked="{Binding EnablePATHSource}"
                    ToolTip="{x:Static root:Localize.Settings_IndexPATH_Tooltip}" />
            </WrapPanel>
        </DockPanel>
        <StackPanel
            Grid.Row="1"
            HorizontalAlignment="Stretch"
            Orientation="Vertical">
            <Separator Style="{StaticResource SettingPanelSeparatorStyle}" />
            <DockPanel HorizontalAlignment="Stretch" LastChildFill="True">
                <TextBlock
                    MinWidth="120"
                    VerticalAlignment="Center"
                    Text="{x:Static root:Localize.Settings_IndexOptions}" />
                <WrapPanel
                    Width="Auto"
                    HorizontalAlignment="Right"
                    DockPanel.Dock="Right">
                    <CheckBox
                        Content="{x:Static root:Localize.Settings_EnableHideLnkPath}"
                        IsChecked="{Binding HideAppsPath}"
                        ToolTip="{x:Static root:Localize.Settings_EnableHideLnkPath_Tooltip}" />
                    <CheckBox
                        Content="{x:Static root:Localize.Settings_EnableHideUninstallers}"
                        IsChecked="{Binding HideUninstallers}"
                        ToolTip="{x:Static root:Localize.Settings_EnableHideUninstallers_Tooltip}" />
                    <CheckBox
                        Content="{x:Static root:Localize.Settings_EnableDescription}"
                        IsChecked="{Binding EnableDescription}"
                        ToolTip="{x:Static root:Localize.Settings_EnableDescription_Tooltip}" />
                    <CheckBox
                        Content="{x:Static root:Localize.Settings_EnableHideDuplicatedWindowsApp}"
                        IsChecked="{Binding HideDuplicatedWindowsApp}"
                        ToolTip="{x:Static root:Localize.Settings_EnableHideDuplicatedWindowsApp_Tooltip}" />
                </WrapPanel>
            </DockPanel>
            <Separator Style="{StaticResource SettingPanelSeparatorStyle}" />
            <StackPanel
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <Button
                    x:Name="btnLoadAllProgramSource"
                    MinWidth="120"
                    HorizontalAlignment="Right"
                    Click="btnLoadAllProgramSource_OnClick"
                    Content="{x:Static root:Localize.Settings_AllPrograms}" />
                <Button
                    x:Name="btnProgramSuffixes"
                    MinWidth="120"
                    HorizontalAlignment="Right"
                    Click="BtnProgramSuffixes_OnClick"
                    Content="{x:Static root:Localize.Settings_ProgramSuffixes}" />
                <Button
                    x:Name="btnReindex"
                    MinWidth="120"
                    HorizontalAlignment="Right"
                    Click="btnReindex_Click"
                    Content="{x:Static root:Localize.Settings_Reindex}" />
                <StackPanel
                    x:Name="indexingPanel"
                    HorizontalAlignment="Left"
                    Orientation="Horizontal"
                    Visibility="Hidden">
                    <ProgressBar
                        x:Name="progressBarIndexing"
                        Width="80"
                        Height="20"
                        IsIndeterminate="True"
                        Maximum="100"
                        Minimum="0" />
                    <TextBlock
                        Height="20"
                        HorizontalAlignment="Center"
                        Text="{x:Static root:Localize.Settings_Indexing}" />
                </StackPanel>
            </StackPanel>
        </StackPanel>
        <ListView
            x:Name="programSourceView"
            Grid.Row="2"
            AllowDrop="True"
            BorderBrush="DarkGray"
            BorderThickness="1"
            DragEnter="programSourceView_DragEnter"
            Drop="programSourceView_Drop"
            GridViewColumnHeader.Click="GridViewColumnHeaderClickedHandler"
            MouseDoubleClick="programSourceView_MouseDoubleClick"
            PreviewMouseRightButtonUp="ProgramSourceView_PreviewMouseRightButtonUp"
            SelectionChanged="programSourceView_SelectionChanged"
            SelectionMode="Extended"
            SizeChanged="ListView_SizeChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="150" Header="{x:Static root:Localize.Settings_ProgramName}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="90" Header="{x:Static root:Localize.Settings_ProgramStatus}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock TextAlignment="Left">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="{x:Static root:Localize.Settings_ProgramDisabled}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Enabled, UpdateSourceTrigger=PropertyChanged}" Value="True">
                                                    <Setter Property="Text" Value="{x:Static root:Localize.Settings_ProgramEnabled}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="{x:Static root:Localize.Settings_ProgramLocation}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Location}" TextTrimming="CharacterEllipsis" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <DockPanel Grid.Row="3">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button
                    x:Name="btnProgramSourceStatus"
                    MinWidth="100"
                    Click="btnProgramSourceStatus_OnClick"
                    Content="{x:Static root:Localize.Settings_ProgramDisable}" />
                <Button
                    x:Name="btnEditProgramSource"
                    MinWidth="100"
                    Click="btnEditProgramSource_OnClick"
                    Content="{x:Static root:Localize.Settings_Edit}" />
                <Button
                    x:Name="btnDeleteProgramSource"
                    MinWidth="100"
                    Click="btnDeleteProgramSource_OnClick"
                    Content="{x:Static root:Localize.Settings_Delete}" />
                <Button
                    x:Name="btnAddProgramSource"
                    MinWidth="100"
                    Click="btnAddProgramSource_OnClick"
                    Content="{x:Static root:Localize.Settings_Add}" />
            </StackPanel>
        </DockPanel>
    </Grid>
</UserControl>
