<UserControl
    x:Class="Flow.Launcher.Plugin.ProcessKiller.Views.SettingsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Flow.Launcher.Plugin.ProcessKiller.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
    d:DesignHeight="300"
    d:DesignWidth="500"
    mc:Ignorable="d">

    <StackPanel>
        <CheckBox
            Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
            Content="Show window titles when possible"
            IsChecked="{Binding Settings.ShowWindowTitle}" />
        <CheckBox
            Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
            Content="Put processes with visible windows on top of the list"
            IsChecked="{Binding Settings.PutVisibleWindowProcessesTop}" />
        <CheckBox
            Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
            Content="Filter service processes (svchost)"
            IsChecked="{Binding Settings.FilterSvchostProcesses}" />
    </StackPanel>
</UserControl>