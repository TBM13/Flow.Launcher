<UserControl
    x:Class="Flow.Launcher.Plugin.ProcessKiller.Views.SettingsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Flow.Launcher.Plugin.ProcessKiller.ViewModels"
    xmlns:root="clr-namespace:Flow.Launcher.Plugin.ProcessKiller"
    d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
    d:DesignHeight="300"
    d:DesignWidth="500"
    mc:Ignorable="d">

    <Grid Margin="{StaticResource SettingPanelMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <CheckBox
            Grid.Row="0"
            Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
            Content="{x:Static root:Localize.Settings_ShowWindowTitle}"
            IsChecked="{Binding Settings.ShowWindowTitle}" />
        <CheckBox
            Grid.Row="1"
            Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
            Content="{x:Static root:Localize.Settings_PutVisibleWindowsOnTop}"
            IsChecked="{Binding Settings.PutVisibleWindowProcessesTop}" />
    </Grid>
</UserControl>